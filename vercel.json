{
    "version": 2,
    "routes": [
        {
            "src": "/invite/[^/]+/[^/]+",
            "dest": "/index.html"
        },
        {
            "src": "/project/[^/]+",
            "dest": "/index.html"
        },
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ],
    "builds": [
        {
            "src": "client/package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist"
            }
        }
    ]
}